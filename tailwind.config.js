const { defaults } = require("autoprefixer");

module.exports = {
  purge: [],
  theme: {
    extend: {
      fontFamily: { display: ["Poppins", "sans-serif"] },
    },
  },
  variants: {},
  plugins: [],
};

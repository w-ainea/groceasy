const { defaults } = require("autoprefixer");

module.exports = {
  purge: [],
  theme: {
    extend: {
      fontFamily: { display: ["Nunito", "sans-serif"] },
    },
  },
  variants: {},
  plugins: [],
};
